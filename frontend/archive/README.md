# Arquivo — páginas legadas

Estas cópias são **apenas referência**. Não as uses em produção.

| Ficheiro | Substituído por | Notas |
|----------|-----------------|-------|
| `index2.html` | `../index.html` | Variante antiga da homepage com CSS/JS duplicado |
| `admin.html` | `../index.html` + `../backoffice.html` | Transcrição + painel admin separados |

Os URLs antigos (`/index2.html`, `/admin.html`) redirecionam automaticamente para `index.html`.

**Página canónica:** `frontend/index.html`
